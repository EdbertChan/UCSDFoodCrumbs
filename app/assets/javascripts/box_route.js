//gets a route and returns an array of boxes

function route_box(google_route){
    var directionService = new google.maps.DirectionsService();
    //log("A test JavaScript Message.");
    return google_route;
}